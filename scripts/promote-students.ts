// import { PrismaClient } from "@prisma/client";

// const prisma = new PrismaClient();

// await prisma.student.updateMany({
//   where: { currentSemester: { lt: 8 } }, // Only promote if not graduated
//   data: { currentSemester: { increment: 1 } }
// });